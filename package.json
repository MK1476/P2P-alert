{
  "name": "p2p-alert",
  "version": "1.0.0",
  "description": "Alerts from Binance P2P",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "next build && npm run build:lambda",
    "build:lambda": "netlify-lambda build functions"

  },
  "author": "MK",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.0.3",
    "https": "^1.0.0",
    "node-binance-api": "^0.13.1",
    "node-fetch": "^3.3.1",
    "twilio": "^4.11.0"
  },
  "devDependencies": {
    "netlify-lambda": "^2.0.16"
  }
}
